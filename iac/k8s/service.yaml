apiVersion: v1
kind: Service
metadata:
  name: loanserve-core
  labels:
    app: loanserve-core
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: loanserve-core